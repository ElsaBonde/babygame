/// <reference path="./entity.ts" />

class Clock extends Entity {
  constructor(
    //image: p5.Image,
    size: number,
    x: number,
    y: number
  ) {
    super(//image,  size, x, y);
  }
  freezeTime(time: Time) {}
  draw() {}
}
